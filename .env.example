# Supabase 接続情報 (本物の値は .env にコピーして使う、.env はコミットしない)

# クローラ用 (USE_SUPABASE=1 でSupabase書き込みモード)
export USE_SUPABASE=1
export SUPABASE_URL="https://your-project.supabase.co"
export SUPABASE_SERVICE_KEY="sb_secret_xxxxxxxxxxxxxxxxxxxx"  # service_role キー (絶対公開禁止)
